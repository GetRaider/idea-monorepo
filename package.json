{
  "name": "denzel",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "author": "Andrew Polovets",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@denzel/eslint-config": "workspace:*",
    "@denzel/typescript-config": "workspace:*",
    "prettier": "^3.5.3",
    "turbo": "^2.5.2"
  },
  "packageManager": "pnpm@10.9.0",
  "engines": {
    "node": ">=20.18.0"
  }
}
