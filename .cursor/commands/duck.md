# 🦆 Duck

Research, reason, and answer — this is a question, not a task. Do not edit, create, or modify any files, and don't propose a diff. If verifying the answer genuinely requires running something (a script, a query), ask first instead of just doing it.

---

## Research (mandatory)

- Check the actual code/config for how this really works today — don't answer from memory or assumption if the answer lives in this repo.
- Trace the real path: follow imports, call sites, and data flow far enough to be sure, not just the first file that looks relevant.
- Check existing rules, README, and any PRD already in the conversation before treating something as undocumented.
- If the answer depends on a library/framework's actual behavior, verify it rather than guessing from general knowledge.

---

## Reasoning

- Show the reasoning that got you to the answer, not just the conclusion — what you looked at, and what you found there.
- If there's more than one plausible answer or a real tradeoff, lay out the options instead of silently picking one.
- If you're genuinely unsure, say so directly instead of presenting a guess as settled.

---

## Boundaries

- No file changes, no "while I'm here" fixes, no implementation — even if the answer obviously implies one. Just say what you'd do, if asked.
- If something you find along the way looks broken or worth flagging, mention it separately at the end, not as a fix.

---

## Output

1. Direct answer, up front
2. Reasoning / evidence (files, code paths, docs actually checked)
3. Open questions or caveats, if any

---

# Question:
