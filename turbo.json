{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "clean": {
      "inputs": ["^clean"],
      "outputs": ["dist/**", ".next/**", ".turbo/**"]
    },
    "clean:hard": {
      "inputs": ["^clean:hard"],
      "outputs": ["dist/**", ".next/**", ".turbo/**", ".node_modules/**"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "tsconfig.build.tsbuildinfo",
        "dist/**"
      ]
    },
    "dev": {
      "dependsOn": ["^build"],
      "env": ["NODE_ENV=development"],
      "cache": false,
      "persistent": true
    },
    "dev:devinity": {
      "inputs": ["^dev:devinity"],
      "env": ["NODE_ENV=development"],
      "cache": false,
      "persistent": true
    },
    "dev:take-and-do": {
      "inputs": ["^dev:take-and-do"],
      "env": ["NODE_ENV=development"],
      "cache": false,
      "persistent": true
    },
    "prod": {
      "env": ["NODE_ENV=production"],
      "dependsOn": ["build"],
      "cache": true,
      "persistent": false
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "codeCheck": {
      "dependsOn": ["^codeCheck"]
    },
    "test": {
      "dependsOn": ["^test"],
      "cache": false
    },
    "test:e2e": {
      "dependsOn": ["^test:e2e"],
      "cache": false
    }
  }
}
