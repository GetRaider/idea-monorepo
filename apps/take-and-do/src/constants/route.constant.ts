export enum Route {
  LOGIN = "/login",
  FORBIDDEN = "/forbidden",
  AUTH = "/auth",
  OVERVIEW = "/overview",
  TASKS = "/tasks",
  TASK_BOARDS = "/task-boards",
  FOLDERS = "/folders",
  LABELS = "/labels",
  STATS = "/stats",
  ANALYTICS = "/analytics",
}
